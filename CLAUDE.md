# A_mealagent_v3 · Agent Rules

## 命名

- 函数动词开头；布尔量用 is_/has_ 前缀

## 目录

- 业务逻辑放 `backend`；测试放 `tests`，文件名 `test_*.py`
- spec模板在 `docs\templates\01_spec模板.md`

## 写代码的口味

- 先 spec、再测试、再实现；一个改动一个小提交
- spec的数据模型在保证功能的基础上最简洁的设计, Spec 只写影响输入、输出、错误或范围且可直接测试的业务约束，不写可推导、重复或内部实现细节。
- 依赖注入发生在Service创建阶段，业务参数传递发生在Service方法调用阶段，两者不得混在同一个公开接口中
- 公共函数要有类型签名
- 错误要抛明确异常，不静默吞掉

## 禁止

- 不要引入未在 requirements 里的新依赖（先问）
- 不要为通过类型检查加无意义的抽象
- 不可逆操作（删数据/动钱/对外发布）必须留给人确认
- 不要兜底fallback，如果要出现兜底情况请说明
- 不要在对话中引用spec

## 测试

- 改了行为就更新/补测试；不许把失败测试注释掉
- spec 与测试双向对齐：改 spec 必须同步对应测试（spec 有规则无测试即视为漂移）；实现新增字段/行为/接口必须回写 spec（spec 过时同样视为漂移）。完成改动后跑一次全量回归确认行为与原来一致
- 测试之前检查相关基础设施是否启动
